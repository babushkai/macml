name = "ralph-github-app"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# Secrets (set via `wrangler secret put <NAME>`):
# - GITHUB_APP_ID
# - GITHUB_PRIVATE_KEY
# - GITHUB_WEBHOOK_SECRET
# - ANTHROPIC_API_KEY

[vars]
ENVIRONMENT = "production"
